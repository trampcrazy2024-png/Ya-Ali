# Ya-Ali / Langua AI — رفع اشکال (۱۷ شهریور ۱۴۰۵)

## ۱. اسکرول ناهموار — `apps/mobile/src/index.css`
علت: طی نسخه‌های مختلف، چند لایه «رفع اسکرول» روی هم انباشته شده بود (از جمله
یک بلوک کاملاً تکراری) که هرکدام مقدار متفاوتی برای overflow، touch-action و
overscroll-behavior روی همان المان‌ها ست می‌کردند. WebView اندروید با این حجم
قوانین متناقض روی یک المان، اسکرول را کِشدار/سخت نشان می‌دهد.
رفع: بلوک تکراری حذف شد و یک بخش نهایی و واحد در انتهای فایل اضافه شد که مدل
اسکرول را ساده و بدون تناقض می‌کند: body تنها مسئول اسکرول صفحه است، و فقط
لیست پیام‌های چت (`.chat-panel .messages`) اسکرول تودرتوی خودش را دارد.

## ۲. دکمه ضبط صدا / خطای عدم دسترسی به میکروفون
سه مشکل واقعی پیدا و رفع شد:

- **AndroidManifest.xml**: بلوک `<queries>` فقط اکشن `RecognitionService` را
  اعلام کرده بود، نه `android.speech.action.RECOGNIZE_SPEECH` که همان اکشنی
  است که پلاگین برای باز کردن رابط تشخیص گفتار سیستمی استفاده می‌کند. روی
  Android 11+ (targetSdk 36 این پروژه) بدون اعلام صریح، سیستم ممکن است این
  اکتیویتی را «نامرئی» فرض کند. اکشن دوم اضافه شد.
- **MainActivity.java**: مجوز میکروفون به‌صورت مستقیم و جدا از سیستم مجوز
  Capacitor در `onCreate` درخواست می‌شد، بدون هندل‌کردن نتیجه
  (`onRequestPermissionsResult` اصلاً override نشده بود). این باعث یک دیالوگ
  مجوز اضافه و ناهماهنگ با جریان واقعی پلاگین NativeSTT می‌شد. حذف شد؛ اکنون
  فقط خود `NativeSTTPlugin` دقیقاً وقتی لازم است مجوز را می‌گیرد.
- **NativeSTTPlugin.java**: چک اولیه‌ی `SpeechRecognizer.isRecognitionAvailable()`
  قبلاً بلافاصله با پیام کلی «در دسترس نیست» رد می‌کرد، حتی روی گوشی‌هایی که
  یک recognizer واقعی داشتند اما این چک کاذب false برمی‌گرداند. حالا برنامه
  همیشه ابتدا تلاش می‌کند (اکتیویتی سیستمی → recognizer مستقیم) و فقط اگر هر
  دو مسیر واقعاً شکست بخورند، پیام دقیق و **کاربردی** نشان می‌دهد که این گوشی
  اصلاً سرویس تشخیص گفتار سیستمی ندارد (خیلی رایج روی گوشی‌های اندرویدی بدون
  Google App/Play Services که در ایران متداول است) و کاربر را به نصب یک مدل
  گفتار آفلاین (Sherpa-ONNX، بخش تنظیمات ← Audio Core) هدایت می‌کند.

**نکته مهم که باید بدانید:** تشخیص گفتار فعلی برنامه به‌طور پیش‌فرض کاملاً به
recognizer سیستمی اندروید (معمولاً Google) وابسته است؛ موتور آفلاین
Sherpa-ONNX از قبل در کد وجود دارد ولی هیچ مدلی به‌صورت پیش‌فرض نصب نیست و
کاربر باید مسیر ۴ فایل مدل را دستی وارد کند. برای کاربرانی که Google
Services ندارند (بخش زیادی از کاربران در ایران) این یعنی میکروفون عملاً کار
نمی‌کند مگر یک مدل آفلاین نصب شود. این مورد در بخش «پیشنهادها» زیر به‌عنوان
اولویت ۱ نقشه راه آورده شده.

## ۳. گسترش بانک واژگان و سناریوها
- `apps/mobile/src/scenarioBankSeed.ts`: حدود ۷۰ واژه پرکاربرد جدید اضافه شد
  (خانواده، احساسات، آب‌وهوا، غذا، فناوری روزمره، تحصیل، مالی، سلامت، خانه،
  مکالمه) با ترجمه و دسته‌بندی.
- `apps/mobile/src/scenarioExpansionV2.ts` (فایل جدید): ۲۰ سناریوی واقعی جدید
  با نقش، هدف، محدودیت و turn-plan در دسته‌های خانواده، اورژانس، فناوری،
  کار، تحصیل، مالی و اجتماعی — نقاط خالی کتابخانه قبلی سناریوها را پر می‌کند.
  در `apps/mobile/src/services/scenarioLibrary.ts` به فهرست اصلی وصل شد.

## فایل‌های تغییر‌یافته
```
apps/mobile/src/index.css
apps/mobile/src/scenarioBankSeed.ts
apps/mobile/src/services/scenarioLibrary.ts
apps/mobile/src/scenarioExpansionV2.ts        (جدید)
android/app/src/main/AndroidManifest.xml
android/app/src/main/java/com/yaali/assistant/MainActivity.java
android/app/src/main/java/com/yaali/assistant/plugins/NativeSTTPlugin.java
```

⚠️ این ابزار (Claude در چت) به شبکه و Android SDK دسترسی ندارد، پس فقط
**کد منبع** اصلاح و در این ZIP تحویل داده شده — APK از قبل build نشده. برای
گرفتن APK جدید همان دستور همیشگی را در Codespace اجرا کنید (بخش ۲۵ گزارش
پیوست‌شده).

---

## بروزرسانی ۲: نصب یک‌کلیکی STT آفلاین + آگاهی از وضعیت دستگاه (طبق توافق روی ترتیب اولویت‌ها)

### ۱. `apps/mobile/src/services/offlineSttSetup.ts` (فایل جدید)
مدل واقعی و تأییدشده `csukuangfj/sherpa-onnx-streaming-zipformer-en-20M-2023-02-17`
(Apache-2.0، پروژه k2-fsa/sherpa-onnx) مستقیماً از Hugging Face — بدون نیاز
به استخراج آرشیو tar.bz2 — روی دستگاه دانلود و مستقیماً با `saveSherpaSttModel`
ثبت می‌شود. حجم کل حدود ۴۲ مگابایت (نسخه‌های int8).

**محدودیت واقعی که باید بدانید:** در فهرست رسمی مدل‌های streaming پروژه
sherpa-onnx فعلاً هیچ مدل فارسی یا عربی منتشر نشده (فقط انگلیسی، چینی،
کره‌ای، فرانسوی، روسی، ژاپنی، ویتنامی...). یعنی این نصب یک‌کلیکی مشکل
میکروفون را **برای تمرین انگلیسی (پرونانسیشن/سناریو)** به‌طور کامل و آفلاین
حل می‌کند، اما دیکته فارسی در نوار پیام چت همچنان به recognizer سیستمی
اندروید وابسته است (طبق بروزرسانی ۱). راه‌حل واقعی فارسی/عربیِ کاملاً
آفلاین، افزودن پشتیبانی از Whisper آفلاین به `SherpaOnnxPlugin.java` است
(که چندزبانه است) — یک تغییر native، نه صرفاً یک دکمه دانلود؛ به‌عنوان کار
بعدی در بخش «ادامه مسیر» زیر آمده.

- `apps/mobile/package.json`: وابستگی `@capacitor/filesystem` اضافه شد (لازم
  برای نوشتن فایل‌های مدل روی دیسک واقعی گوشی؛ برخلاف `voicePackManager`ی
  که از قبل بود و صداها را در IndexedDB نگه می‌دارد — جایی که کد native
  اصلاً نمی‌تواند آن را با یک مسیر فایل باز کند).
- `apps/mobile/src/App.tsx`: در تنظیمات، بالای بخش «Audio Core» یک کارت
  «نصب یک‌کلیکی گفتار آفلاین» اضافه شد با یک دکمه، نوار پیشرفت درصدی و دکمه
  حذف؛ بخش قدیمی ثبت دستی مسیر فایل به «پیشرفته» تغییر نام گرفت و برای
  کاربرانی که مدل‌های سفارشی (مثلاً عربی/فارسی خودساخته) دارند باقی ماند.

### ۲. آگاهی از وضعیت دستگاه (`apps/mobile/src/speech.ts`: `getVoiceReadiness`)
تابع سبکی که وضعیت واقعی دستگاه را برمی‌گرداند: آیا recognizer سیستمی هست،
آیا مدل آفلاین انگلیسی نصب است، و آنلاین بودن. این یک ویزارد یا صفحه جدید
نیست — دقیقاً طبق چیزی که خواستید («بهترین مسیر را خودش پیشنهاد دهد»)، فقط
یک بار در اولین اجرا (اگر گوشی نه recognizer سیستمی دارد نه مدل آفلاین)
یک toast راهنما نشان می‌دهد، و همین منطق برای پیام‌های خطای دکمه میکروفون
هم استفاده می‌شود تا پیام همیشه «چه کاری الان انجام بده» را به کاربر بگوید،
نه فقط «کار نکرد».

### ادامه پیاده‌سازی — مورد ۳ و شروع ۴ (طبق همان توافق)

پس از بررسی کد، معلوم شد بخش زیادی از معماری «Evaluation → Skill Vector →
Memory» (بخش‌های ۱۹، ۳۹-۴۰ گزارش پیوستی) از قبل به‌صورت منطق خالص در
`packages/core/src/learning/*` و `packages/database` نوشته و حتی تست هم
شده بود — اما دو نقطهٔ اتصال واقعی خراب/ناقص بود:

- **مشکل ۱ (مهم‌تر):** ارزیابی هر پیام (`evaluateScenarioResponse`) فقط وقتی
  اجرا می‌شد که کاربر از تب «سناریوها» وارد شده باشد (`if (selectedScenario)`).
  یعنی همان چیزی که قرار است ستون فقرات محصول باشد — «هر مکالمه ارزیابی
  می‌شود» — در عمل روی مکالمهٔ آزاد تب چت (که همان چیزی است که کاربر واقعی
  ۹۹٪ وقت با آن کار می‌کند) هرگز اجرا نمی‌شد. در `App.tsx` این شرط برداشته
  شد؛ حالا هر پیام (چه در سناریو، چه در گفتگوی آزاد) ارزیابی، Skill Vector
  به‌روزرسانی و در دیتابیس ذخیره می‌شود. این کار در پس‌زمینه و غیرمسدودکننده
  انجام می‌شود (UI معطل نمی‌ماند)، اما یک تولید (generation) اضافه توسط مدل
  در پس‌زمینه یعنی مصرف باتری/CPU بیشتر—به‌خصوص روی مدل محلی—که آگاهانه و
  مطابق اولویت شما پذیرفته شد.
- **مشکل ۲:** خروجی `corrections` که ارزیابی تولید می‌کرد هرگز به حافظهٔ
  محلی مکالمه (`localLearningMemory.ts`، لایهٔ L1/context prompt) بازنمی‌گشت؛
  فیلد `recentCorrections` همیشه خالی می‌ماند چون هیچ‌کس مقدارش نمی‌داد. تابع
  جدید `recordLocalCorrection` اضافه شد و حالا بعد از هر ارزیابی، اولین
  اصلاح واقعی مستقیماً وارد حافظهٔ همان لحظه‌ای می‌شود که در System Prompt
  نشست بعدی استفاده می‌شود.
- **پیشنهاد پیشگیرانه («سیستم خودش تصمیم بگیرد»):** یک بنر کوچک در بالای تب
  چت اضافه شد که با خواندن Skill Vector ذخیره‌شده، ضعیف‌ترین مهارت را نشان
  می‌دهد و با یک دکمه («شروع این تمرین») مستقیماً یک تمرین اختصاصی از همان
  موتور `generateAndPersistAdaptiveScenario` که قبلاً فقط پشت یک دکمهٔ پنهان
  در تب سناریوها بود می‌سازد — دقیقاً همان مثال بخش ۳۸ گزارش («امروز ۱۰
  دقیقه با هم تمرین کنیم؟ … ضعف مشاهده‌شده: …»).

**چرا اینجا متوقف شدم:** حافظهٔ L1/L2/L3 کامل (تفکیک صریح context/summary/
long-term با فشرده‌سازی خودکار) و تبدیل «حافظهٔ محلی سبک فعلی» به یک لایهٔ
معماری رسمی، و همچنین تعمیم الگوی دانلود خودکار به Voice Packها و تست روی
دستگاه واقعی (موارد ۴ دوم، ۵ و ۶ شما)، به تصمیم‌های محصولی و تست واقعی نیاز
دارند که بدون گوشی/امکان اجرا نمی‌توانم آن‌ها را با اطمینان کامل کنم. آنچه
همین حالا اضافه شد (بستن حلقهٔ ارزیابی + بازخورد اصلاحات + پیشنهاد پیشگیرانه)
از نظر من بیشترین ارزش را با کمترین ریسک به سیستم فعلی اضافه می‌کند؛ برای
ادامه (خصوصاً L1/L2/L3 رسمی) بهتر است بعد از تست همین نسخه روی گوشی واقعی
پیش برویم.

فایل‌های اضافه‌شده به فهرست تغییرات این بروزرسانی:
```
apps/mobile/src/services/localLearningMemory.ts   (recordLocalCorrection)
apps/mobile/src/App.tsx                           (ارزیابی همیشگی + بنر پیشنهاد)
```

---

## بروزرسانی ۳: حافظهٔ واقعی L1 / L2 / L3 + جداسازی ارزیابی سریع/عمیق

طبق توافق، قبل از تست روی گوشی، حلقهٔ حافظه کامل شد. سه فایل جدید:

- **`packages/core/src/learning/memoryTiers.ts`** (منطق خالص، بدون وابستگی به دیتابیس):
  `buildL1Context` (سقف هم روی تعداد پیام‌ها هم روی مجموع کاراکترها — برخلاف
  `.slice(-N)` قبلی که با چند پیام خیلی بلند همچنان بودجهٔ توکن را می‌ترکاند)،
  `shouldSummarizeSession`/`buildSummaryPrompt`/`parseSessionSummary` برای L2،
  و `shouldPromoteToLongTerm`/`importanceForOccurrences`/`normalizeFactKey`
  برای دروازهٔ ورود به L3 (فقط با تکرار ≥۲، طبق قانون ضدآلودگی حافظهٔ خودتان).
  همچنین `evaluateTurnFast` — ارزیابی سبک و بدون فراخوانی مدل.
- **`apps/mobile/src/services/memoryTiers.ts`**: اتصال منطق بالا به ذخیره‌سازی
  واقعی. L2 (خلاصهٔ نشست) در `localStorage` به ازای هر مکالمه نگه داشته
  می‌شود. L3 برای اولین‌بار از `UserMemoryRepository` (جدولی که در دیتابیس
  از قبل تعریف شده بود ولی هیچ‌جای اپ استفاده نمی‌شد!) استفاده می‌کند —
  یک key/value با فیلد `importance`، دقیقاً مناسب حافظهٔ بلندمدت.
- **`apps/mobile/src/services/localLearningMemory.ts`**: شمارندهٔ سبک تکرار
  (`bumpFactOccurrence`) اضافه شد تا تشخیص «این اشتباه/موضوع قبلاً هم دیده
  شده» بدون فراخوانی دیتابیس در هر پیام ممکن باشد.

### چطور حلقه الان کار می‌کند (در `App.tsx`)
- **L1**: به‌جای `history.slice(-10)`/`.slice(-12)` قدیمی، الان `buildL1Context`
  هم تعداد پیام و هم مجموع کاراکتر را محدود می‌کند.
- **L2**: هر ۱۶ پیام یک‌بار (`shouldSummarizeSession`)، بخش قدیمی مکالمه
  خلاصه می‌شود (فقط برای Prompt؛ چیزی از صفحهٔ چت کاربر حذف نمی‌شود) و در
  `localStorage` ذخیره می‌گردد؛ اصلاحات استخراج‌شده از خلاصه هم وارد مسیر
  ترفیع به L3 می‌شوند.
- **L3**: یک موضوع یا اشتباه فقط از بار دوم به بعد وارد `user_memory`
  می‌شود (`shouldPromoteToLongTerm`)؛ یک اشتباه یک‌باره هرگز به دیتابیس
  نمی‌رسد — دقیقاً قانون بخش ۵ شما.
- **ارزیابی سریع/عمیق (بخش ۶ شما):** حالا برخلاف بروزرسانی قبلی (که هر پیام
  آزاد هم یک ارزیابی عمیق با مدل می‌گرفت)، مکالمهٔ آزاد فقط `evaluateTurnFast`
  (بدون فراخوانی مدل) می‌گیرد و Skill Vector را فوراً به‌روز می‌کند؛ ارزیابی
  عمیق واقعی (با مدل) فقط در همان نقاط چک‌پوینت L2 (هر ۱۶ پیام) اجرا می‌شود.
  حالت سناریوی تمرینی همچنان مثل قبل هر پیام ارزیابی عمیق می‌گیرد (بدون
  تغییر، چون تمرین هدفمند و محدود است).
- بنر پیشنهادی که در بروزرسانی قبلی اضافه شده بود دست‌نخورده ماند؛ حالا
  دقیق‌تر کار می‌کند چون Skill Vector دیگر فقط از تب سناریو به‌روز نمی‌شود.

### محدودیت صادقانه‌ای که باید بدانید
تشخیص «این اشتباه/موضوع تکراری است» بر پایهٔ برابری متن نرمال‌شده است
(`normalizeFactKey`: حروف کوچک + حذف علائم + یکسان‌سازی فاصله)، نه شباهت
معنایی واقعی. یعنی «فعل گذشتهٔ go را اشتباه صرف کردی» و «go را در زمان
گذشته درست نگفتی» دو رشتهٔ متفاوت شمرده می‌شوند و هرکدام باید جدا تکرار
شوند تا به L3 برسند. این یک محدودیت شناخته‌شده است، نه باگ — رفع کامل آن
به تطبیق معنایی (embedding) نیاز دارد که در این پاس اضافه نشده.

فایل‌های تغییریافته/جدید این بروزرسانی:
```
packages/core/src/learning/memoryTiers.ts     (جدید)
packages/core/src/index.ts                    (export)
apps/mobile/src/services/memoryTiers.ts       (جدید)
apps/mobile/src/services/localLearningMemory.ts
apps/mobile/src/App.tsx
```

---

## بروزرسانی ۴: رفع خطاهای واقعی کد + شروع LAILI Phase 1 (Learner Model)

### رفع خطا (طبق درخواست «تمام خطاها را برطرف کن»)
بدون `npm install` و `tsc` واقعی (این محیط شبکه ندارد) نمی‌توانم کل مونوریپو
را کامپایل کنم، اما `tsconfig.json` پروژه شما تنظیمات بسیار سخت‌گیرانه‌ای
دارد (`strict`, `noUnusedLocals`, `noUncheckedIndexedAccess`,
`exactOptionalPropertyTypes`) — یک جست‌وجوی هدفمند برای رایج‌ترین کلاس خطا
زیر این تنظیمات (`exactOptionalPropertyTypes`) در کل apps/mobile،
packages/core و packages/database زدم و این موارد **واقعی و از قبل موجود**
(نه از تغییرات من) را پیدا و رفع کردم:

- `apps/mobile/src/ai.ts`: نوع `DiscoveredModel.contextWindow` مقدار
  `number|undefined` می‌گرفت ولی به‌صورت `contextWindow?:number` (بدون
  `|undefined`) تعریف شده بود — خطای کامپایل زیر `exactOptionalPropertyTypes`.
  اصلاح شد به `contextWindow?:number|undefined`.
- `apps/mobile/src/App.tsx` (ثبت دستی مدل TTS): همین الگو برای `dataDir`.
  به‌جای فیلد را با مقدار `undefined` صریح پر کردن، حالا فقط وقتی کاربر
  مقداری وارد کرده، کلید اصلاً اضافه می‌شود.
- `packages/database/package.json`: این پکیج از قبل کد داخلش
  (`AdaptiveLearningRepository.ts`) از `@yaali/core` تایپ import می‌کرد، ولی
  `@yaali/core` هرگز در `dependencies` این پکیج اعلام نشده بود — یک وابستگی
  گمشده در مونوریپو. اضافه شد.

نتوانستم (و صادقانه نمی‌توانم بدون توانایی اجرای `npm run typecheck` روی
دستگاه واقعی) قول بدهم که **تمام** خطاهای احتمالی در حدود ۱۵٬۰۰۰+ خط کد پیدا
شده‌اند؛ توصیه می‌کنم بعد از `npm install`، خودتان `npm run typecheck` را
اجرا کنید (این اسکریپت از قبل در package.json هست) تا هر خطای باقی‌مانده را
با دقت کامل (که فقط یک کامپایلر واقعی می‌تواند بدهد) ببینید.

### LAILI — چرا فقط Phase 1
گزارش پیوستی خودش می‌گوید توسعه باید مرحله‌ای و Backward-Compatible باشد
(بخش‌های ۳۵-۳۶ همان گزارش) و RC1 نباید بشکند. پیاده‌سازی کامل LAILI (۴۰ بخش،
موتور ارزیابی دو-لایه، Conversation Engine هدف‌محور با Information Gap،
Interference Engine، Activation UX، CEFR، Knowledge Tracing، Contextual
Bandit، RL) در یک پاس، هم با اصل خود گزارش در تضاد است، هم بدون امکان
build/تست واقعی من ریسک بسیار بالایی دارد. فقط **Phase 1 — Learner Model**
را کامل و امن پیاده کردم؛ بقیه فازها عمداً برای گام‌های بعدی گذاشته شدند.

**چه چیزی الان واقعاً کار می‌کند:**
- `packages/database/src/migrations/v010_laili_phase1.ts` (Migration جدید،
  کاملاً افزایشی — هیچ جدول قدیمی را لمس نمی‌کند): دو جدول جدید
  `learner_skill_state` (هفت بعد بخش ۷ گزارش: recognition, recall,
  controlled_production, free_production, contextual_use, automatic_use,
  confidence — به ازای هر learner+skill) و `skill_evidence` (لاگ Evidence
  بخش ۹).
- `packages/core/src/learning/learnerModel.ts`: منطق خالص —
  `updateSkillDimensions` (فرمول Weighted EMA بخش ۲۰: New = Old×0.7 +
  Evidence×Confidence×0.3، دقیقاً همانی که گزارش «نسخه ۱، نه قانون نهایی»
  خوانده) و `productionGap` (بخش ۱۵: Recognition − Free Production).
- `packages/database/src/repositories/LearnerSkillStateRepository.ts` +
  `apps/mobile/src/services/learnerModelSync.ts`: هر بار که یک ارزیابی عمیق
  (LLM) — چه در سناریو، چه در چک‌پوینت L2 مکالمهٔ آزاد — تمام می‌شود، حالا
  علاوه بر جدول قدیمی `skill_vectors`، یک رکورد Evidence هم در جدول جدید
  ثبت و ابعاد آن مهارت به‌روزرسانی می‌شود.

**نکتهٔ صادقانهٔ مهم:** این کار داده را **تولید** می‌کند، اما فعلاً هیچ‌جای
اپ آن را **نمی‌خواند**. یعنی Adaptive Engine (دکمهٔ «ساخت سناریوی اختصاصی»،
بنر پیشنهادی تب چت) همچنان فقط از `skill_vectors` قدیمی (تک‌بعدی) استفاده
می‌کند — دقیقاً چون تعریف اینکه «موتور adaptive جدید دقیقاً چطور از این هفت
بعد تصمیم بگیرد» یک تصمیم محصولی مهم است (بخش‌های ۱۶-۱۸ گزارش: Rule-based →
Weighted Scoring) که بهتر است جداگانه و با دقت طراحی شود، نه به‌عنوان یک
اثر جانبی این پاس. جدول‌ها و داده آماده‌اند؛ موتوری که از آن‌ها تصمیم بگیرد
هنوز نه.

### آنچه از این گزارش عمداً پیاده نشد (و چرا)
| بخش گزارش | وضعیت |
|---|---|
| Tier 1 (Deterministic) / Tier 2 (Semantic) Evaluation | نشد — ارزیابی فعلی تک‌لایه (سریع اکتشافی + عمیق LLM) است، نه موتور قانون‌محور deterministic جدا |
| Conversation Engine هدف‌محور + Information Gap + Pushed Output | نشد — مکالمه هنوز آزاد است، نه Task-based با Constraint/Gap صریح |
| Interference Engine + Contrastive Practice | نشد |
| Activation UX (نوار Passive/Active) | نشد — طبق تصمیم قبلی همین گفت‌وگو («فعلاً UI جدید نسازیم») |
| CEFR mapping | نشد |
| Knowledge Tracing / Contextual Bandit / RL (Phase 8-10) | نشد — گزارش خودش می‌گوید این‌ها به داده و مدل پایدار نیاز دارند که هنوز نداریم |

فایل‌های این بروزرسانی:
```
packages/database/src/migrations/v010_laili_phase1.ts   (جدید)
packages/database/src/repositories/LearnerSkillStateRepository.ts (جدید)
packages/database/src/index.ts
packages/database/package.json
packages/core/src/learning/learnerModel.ts               (جدید)
packages/core/src/index.ts
apps/mobile/src/services/learnerModelSync.ts             (جدید)
apps/mobile/src/languageBank.ts
apps/mobile/src/App.tsx
apps/mobile/src/ai.ts
```




