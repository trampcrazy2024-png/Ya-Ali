# Ya-Ali 1.0.3 — Release Status

## این نسخه

- versionName: `1.0.3`
- versionCode: `11`
- minSdk: `24`
- target/compile API: `36`

## اصلاحات اصلی

- بازطراحی لایه scroll موبایل و safe-area؛ اسکرول صفحه و اسکرول پیام‌ها از هم تفکیک شد.
- bottom navigation از 7 به 8 بخش ارتقا یافت و روی عرض‌های کوچک قابل پیمایش افقی است.
- بخش «سناریو» با سناریوهای نقش‌محور برای سفر، کار، خدمات، سلامت، آموزش، خانه، مالی، فناوری، مذاکره، موقعیت حساس و زندگی روزمره اضافه شد.
- سناریوها به‌جای دیالوگ‌های ثابت، قرارداد رفتاری چندمرحله‌ای دارند تا Local AI مکالمه را بر اساس پاسخ واقعی کاربر بسازد.
- حافظه یادگیری Local AI برای تطبیق موضوع، لهجه، اصلاحات و عبارت‌های مفید اضافه شد.
- Native STT در API 31+ در صورت دسترس بودن از `createOnDeviceSpeechRecognizer` استفاده می‌کند.
- minSdk واقعی پروژه با مستندات به `24` همسان شد.
- provenance برای داده‌های آینده مستند شد؛ Tatoeba/OPUS/Common Voice بدون کپی کورکورانه یا ادعای مجوز واحد بررسی شده‌اند.

## زنجیره انتشار

`Source 1.0.3 → npm build → cap sync → Gradle APK versionCode 11 → نصب APK جدید → UI/STT/Diagnostics`

## محدودیت محیط فعلی

در محیط تحویل حاضر وابستگی‌های npm به‌طور کامل نصب نشدند و `npm ci` در سقف زمانی محیط متوقف شد؛ بنابراین build نهایی APK در همین محیط قابل اثبات نیست. قبل از انتشار GitHub باید build واقعی روی CI یا محیط Android مجهز اجرا و artifact و checksum ثبت شود.
